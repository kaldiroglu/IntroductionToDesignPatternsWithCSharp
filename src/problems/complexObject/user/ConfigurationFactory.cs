namespace dev.kaldiroglu.dp.intro.problems.complexObject.user;

public class ConfigurationFactory
{
    public Configuration? CreateConfigurationFromRequest(string configurationEntityId, string? username, string? userEntityId,
                                                         string? remoteAddr, string? header)
    {
        Configuration? configuration = null;
        //
        return configuration;
    }
}
