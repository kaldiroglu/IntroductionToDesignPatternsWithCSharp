namespace dev.kaldiroglu.dp.intro.problems.complexObject.user;

/// <summary>
/// Faithful port of the Java stub: the body is intentionally left unimplemented
/// (it returns null) — this is the "before" picture the course refactors.
/// </summary>
public class UserFactory
{
    public User? CreateUserFromRequest(NewUserRequest userRequest, string? username, string? userResourceId,
                                       string? remoteAddr, string? userAgent)
    {
        User? user = null;
        // ...
        return user;
    }
}
